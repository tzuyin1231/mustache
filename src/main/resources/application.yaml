spring:
  datasource:
    url: jdbc:postgresql://localhost:32770/mustache
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  sendgrid:
    api-key:


